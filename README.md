# Recommendation .NET Application

This is a website that contains reviews about Hotels, restaurants ... It uses Box match recommender algorithm that it's set up with Microsoft Azure Machine Learning.